# Football Pulse score display fix

Replace these two files in GitHub:

- `server.js`
- `app.js`

The new server correctly reads scores supplied as:

- `3 - 0`
- `3-0`
- `3:0`
- separate home and away score fields
- nested `scores.score` values

After committing the files, wait for Render to redeploy and refresh the website.
